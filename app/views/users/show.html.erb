<%= js add_gritter(flash[:notice], :title => notice, :image => :success) %>

<h1><%= @user.name %></h1>

<p>
  <b>E-Mail:</b>
  <%= @user.email %>
</p>

<div class="tags">
  <% @user.skills.each do |tag| %>
    <%= link_to tag.name, { :controller => :tags, :action => :show, :id => tag.id }, :class => "tag3" %>
  <% end %>
</div>

<div class="similar-users">
  <h2>Ähnliche Interessenten</h2>
  <% if @similar_users.empty? %>
     Sorry, keine Übereinstimmungen gefunden!
  <% else %>
    <div class="users-list">
      <ul>
        <% @similar_users.each do |user|%>
          <li>
            <span class="profile-avatar"><%= link_to image_tag(avatar_url(user.email), alt: "?"), user, :title => user.name, :rel => "tipsy" %></span>
          </li>
        <% end %>
      </ul>
    </div>
  <% end %>
</div>
